# -*- coding: utf-8 -*-
"""
config.py —— 所有"会变"的东西都放这里，方便随时改，不用翻代码。

==========================================================
登录方案（推荐）：
  脚本直接用"账号+密码+验证码"完整登录，自动进入选课系统，
  不再需要手动复制 Cookie。（登录会踢掉浏览器里同账号的会话）
==========================================================
"""

# ============ 你需要自己填的部分 ============

# 教务系统域名（就是登录页网址里的主机名部分）
BASE_URL = "https://jwxt.sut.edu.cn"

# 你的学号和密码（完整登录用；也可以运行时输入）
USERNAME = ""
PASSWORD = ""

# 浏览器里"已登录"的 Cookie（旧方式，可选）
# 如果想用 Cookie 登录而不是账号密码，把下面填上；
# 留空则脚本自动走"账号+密码+验证码"完整登录。
# 获取方法见 guides/拿Cookie.md
COOKIE_STR = ""


# ============ 抢课配置（已从抓包确认） ============

GRAB_URL = "https://jwxt.sut.edu.cn/jsxsd/xsxkkc/ggxxkxkOper"

# 目标课程参数 —— ⚠️ 改成你想抢的那门课！
# 当前是"你测试点的那门课"的值。换成目标课方法见下方说明。
# 进入选课页，右键点你想抢的那门课按钮 → "检查"(Inspect)，
# 在它的 onclick 或 <a> 链接里找到 kcid 和 jx0404id 的值填进来。
GRAB_PARAMS = {

}

# 选课页的 Referer（抓包拿到，接口会校验）
GRAB_REFERER = "https://jwxt.sut.edu.cn/jsxsd/xsxkkc/comeInGgxxkxk"

# 可选：定时开抢，格式 "2026-01-01 09:00:00"，留空则脚本立即开始
START_TIME = ""

# ============ 防封 / 并发配置（可自行调整） ============
# 两次抢课请求之间随机间隔的【最短/最长】秒数。
# 越慢越安全但抢得越慢；现在默认较稳的区间，不建议低于 0.3。
INTERVAL_MIN = 0.6
INTERVAL_MAX = 1.5

# 并发抢课的线程数上限（多类别同时抢用）。
# 越大越快但请求越密集、越容易触发风控，默认 3，不建议超过 5。
MAX_THREADS = 3

# 当前默认同一任务顺序提交。以下额度只计算选课提交，不含读取列表。
MANUAL_COURSE_ATTEMPTS = 10  # 手动每门最多 10 次（代码硬上限 30）
AUTO_COURSE_ATTEMPTS = 5     # 自动每门最多 5 次（代码硬上限 30）
ROUND_SUBMIT_LIMIT = 30     # 整轮所有课程合计最多 30 次（代码硬上限 100）

# 连续返回异常多少次后开始"指数退避"（每次间隔翻倍，最多 8 倍），
# 防止被当成刷接口的攻击行为。
BACKOFF_AFTER = 5
